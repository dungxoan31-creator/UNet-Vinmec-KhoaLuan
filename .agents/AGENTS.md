# AGENTS.md — Ovarian Ultrasound AI Decision Support System (KLTN)
## Workspace: `c:\Users\PeaceD_Dung\Documents\Khóa luận`

> **Last Updated:** 2026-08-29 | **Generated by:** Antigravity Engineering Orchestrator

> **GLOBAL RULE IN EFFECT:** `~/.gemini/config/rules/00-GLOBAL-ENGINEERING-RULE.md`
> All tasks in this workspace are governed by: **Maximum capability + Minimum redundancy + Controlled autonomy + Verifiable results.**
> Resource priority: Native Tools → Agents → Skills → MCP → Plugins → Hooks → CLI → Models

---

## 1. PROJECT OVERVIEW

**Name:** Ovarian Ultrasound AI Decision Support System  
**Version:** 1.2.0  
**Domain:** Healthcare + Medical AI (Class II SaMD)  
**Tech Stack:** Python 3.12 + FastAPI + PyTorch + MONAI + Vanilla JS SPA  

**Purpose:** Clinical decision support for ovarian tumor detection, segmentation, measurement (D1/D2/D3/Volume), and risk stratification using ACR O-RADS US v2022 and IOTA Simple Rules on 2D ultrasound.

---

## 2. ARCHITECTURE SUMMARY

```
Backend:  FastAPI (port 8000)  →  Uvicorn
Database: SQLite (ovarian_ai.db via SQLAlchemy 2.0)
Model:    Attention U-Net (checkpoints/best_attention_unet.pth, 31.5MB)
Dataset:  MMOTU 1,372 pairs (OTU_2D + OTU_CEUS) from Beijing Shijitan Hospital
Frontend: Vanilla JS SPA (frontend/) — 4 screens, dual canvas, curtain slider
```

---

## 3. KEY COMMANDS

```bash
# Start dev server (already running on port 8000)
.venv\Scripts\uvicorn.exe backend.app.main:app --host 127.0.0.1 --port 8000

# Run all tests (43 tests)
.venv\Scripts\python.exe -m pytest tests/ -q

# Lint check
ruff check backend/ tests/

# Lint + auto-fix
ruff check backend/ tests/ --fix

# Docker build
docker build -t ovarian-ai .

# Docker compose
docker-compose up -d
```

---

## 4. DIRECTORY STRUCTURE

```
backend/
├── app/          — FastAPI app, 6 routers
├── core/         — Config, security utilities
├── db/           — Database models, session
├── models/       — PyTorch model definitions
├── schemas/      — Pydantic v2 request/response schemas
└── services/     — Business logic services
    ├── preprocessor.py          — Image IQA + preprocessing
    ├── inference_engine.py      — Neural inference + caliper extraction
    ├── model_service.py         — Model registry + adapters
    ├── clinical_nlp_service.py  — Knowledge base + CDSS reasoning
    ├── report_generator.py      — PDF report generation
    ├── backup_service.py        — Database backup
    └── morphology_extractor.py  — Morphological feature extraction

tests/                  — 43 automated tests (10 files)
ai_training/            — Training scripts, evaluation, splits
knowledge/              — Medical guidelines JSON (O-RADS, IOTA, ESGO)
checkpoints/            — Model weights
dataset/                — MMOTU dataset (1,372 pairs)
scripts/                — Utility and analysis scripts
docs/                   — Architecture docs, thesis proposal
```

---

## 5. ANTIGRAVITY MEMORY ARCHITECTURE

> **IMPORTANT:** Two memory systems exist — they are intentionally isolated.

| System | Scope | Primary Use |
|---|---|---|
| **Mem0 local Python** (`memory-env/mem0_local.py`) | Global, persistent across all sessions | Long-term preferences, architecture decisions, project learnings |
| **MCP memory server** (`@modelcontextprotocol/server-memory`) | Session-level external | Used by external MCP clients; do NOT use for primary memory |

**Rule:** Always write to Mem0 local Python for persistent memory. Do not rely on MCP memory server for cross-session retention.

**Mem0 command:**
```powershell
& "C:\Users\PeaceD_Dung\.gemini\memory-env\Scripts\python.exe" `
  "C:\Users\PeaceD_Dung\.gemini\memory-env\mem0_local.py" `
  add "<content>" "<category>" "<project>"
```
Categories: `preferences`, `architecture`, `conventions`, `learnings`

---

## 6. SKILL ROUTING FOR THIS PROJECT

| Task Type | Primary Skill | Secondary |
|---|---|---|
| Medical AI / segmentation | `role-medical-ai` | `systematic-debugging` |
| Backend API / FastAPI | `role-backend` | `api-design`, `tdd` |
| Frontend / UI | `role-frontend` | `ui-ux-pro-max` |
| Testing / QA | `role-qa-qc` | `test-cases`, `playwright-gen` |
| Security | `security-audit` | `role-qa-qc` |
| Debugging | `systematic-debugging` | — |
| Documentation | `userguide` | `diagram-design` |
| Architecture | `role-architect` | `d2-architect` |

---

## 7. TESTING GATES (MANDATORY)

All changes MUST pass before reporting DONE:

| Gate | Command | Expected |
|---|---|---|
| **Lint** | `ruff check backend/ tests/` | 0 errors |
| **Unit+Integration** | `pytest tests/ -q` | 43/43 PASS |
| **E2E** | Included in pytest (`test_user_flow_e2e.py`) | PASS |
| **Typecheck** | NOT CONFIGURED (mypy not installed) | N/A |
| **Security** | Manual + `bandit` if available | No HIGH findings |
| **Build** | `docker build .` | Success |

> **If any gate fails:** Diagnose → Fix → Retest. Max 3 retry loops. If still failing, report blocker explicitly.

---

## 8. GIT SAFETY RULES

1. Always check `git status` before modifying code
2. Never run `git reset --hard` or `git clean -fd` without explicit user approval
3. Never overwrite uncommitted user changes
4. Current state: **40+ files modified/untracked** — user must commit before any destructive operation
5. Two commits on master: `acc409e` (latest) and `8f64001` (initial)

---

## 9. CRITICAL ISSUES (P0/P1 Known)

| Priority | Issue | Status |
|---|---|---|
| P0 | 40+ uncommitted files | ⚠️ User action required |
| P1 | Model adapters (S4M/DS2Net/UltraSAM/SovaSeg) return `{}` | ⚠️ Needs model checkpoints |
| P1 | CPU-only PyTorch (no CUDA) — 130ms inference | ℹ️ Hardware limitation |
| P1 | SQLite under concurrent writes | ℹ️ Development-acceptable |
| P2 | Qdrant installed but not integrated | ℹ️ Reserved for future RAG |
| P2 | uv.lock essentially empty | ℹ️ Use requirements.txt |

---

## 10. CONVENTIONS

- **Language:** Python 3.12 (typed where possible)
- **Linter:** ruff (configured in pyproject.toml)
- **Line length:** 120 chars max
- **Import style:** isort-compatible, `backend` as first-party
- **Test framework:** pytest + pytest-asyncio (asyncio_mode="auto")
- **API style:** RESTful FastAPI with Pydantic v2 validation
- **Frontend:** Vanilla JS ES6 modules — no build step, no TypeScript
- **Logging:** Python stdlib logging (structlog recommended for future)
- **Secrets:** Never committed — check `.gitignore`
